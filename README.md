Hecho por grupo GOON SQUAD
<-Antes de ejecutar->
1.Tener una camara externa conectada a la computadora, NO utiliza la camara integrada de laptops
<-Como ejecutar->
1. Solo ejecuta el archivo .bat llamado "Ejecutador"
<-Qué sucederá->
1. El ejecutador comprobara si se tiene las siguientes cosas: Python, modelo YOLO, camara externa
2.Si no se tienen los programas, se descargaran, pero si no se tiene la camara, el programa no se ejecutara
3. Si se cumple todo lo pedido anteriormente, se ejecutará el programa.
